\subsection{Experiment 3: Reconstruction}

The AUDA analysis table is highly sparse due to substantial missing data across countries, regions, and years. For example, the United States Environmental Protection Agency (EPA) reports plastic waste generation data only at five-year intervals between 2000 and 2015. Therefore, in Experiment 3, we reconstruct the missing values in the \texttt{YearTRC (Japan)} and \texttt{YearTRC (US)} datasets.

The experiment compares simple interpolation baselines with statistical and machine-learning reconstruction models. Linear interpolation, moving average interpolation, and cubic spline interpolation provide reference methods for filling interior gaps. Ridge regression, Gaussian process regression (GPR), support vector regression (SVR), and Theil--Sen regression test whether fitted regression models can better capture the local trend in sparse country-level series.

We randomly select 10\% of the observed data points in each dataset, with at least two points, as the test set and fit the models using the remaining data points. The test samples are guaranteed not to include the first or last data point in the time series, ensuring that the models are evaluated on their interpolation capabilities rather than extrapolation. Since the evaluation metrics are sensitive to the specific test samples selected by the random number generator, we repeat the experiment 16 times and report the average results. Because both datasets have only 9 data points, each run uses only two held-out observations for testing. In this case, the denominator of \(R^2\) is very small, and modest absolute errors can produce a large negative \(R^2\). For this reason, the \(R^2\) metric is not very meaningful in this case, and we do not report it.

\input{4_experiments/tables/reconstruction_japan_trc.tex}
\input{4_experiments/tables/reconstruction_united_states_trc.tex}

% Codex
% TODO: rewrite this
Table~\ref{tab:reconstruction_japan_trc} shows that SVR performs best on the \texttt{YearTRC (Japan)} reconstruction task, achieving the lowest MAE, RMSE, WAPE, and MAPE among the evaluated methods. Its WAPE is 1.32\%, compared with 1.48\% for cubic spline interpolation and 1.59\% for linear interpolation. This suggests that the Japan series benefits from a flexible nonlinear model, although the small gap between SVR and the interpolation baselines also shows that the series is smooth enough for simple interpolation to remain competitive.

% Codex
% TODO: rewrite this
Table~\ref{tab:reconstruction_united_states_trc} shows a slightly different pattern for the \texttt{YearTRC (US)} task. Theil--Sen regression gives the best overall result, with a WAPE of 1.17\%, while linear interpolation is very close at 1.19\%. Ridge regression and SVR are also competitive, both with WAPE near 1.28\%. These results indicate that the United States series is easier to reconstruct with smoother trend-based methods, and that the best model can vary across countries even when the target variable and reconstruction setup are similar.

\input{4_experiments/plots/reconstruction_plots.tex}

% Codex
% TODO: rewrite this
Figure~\ref{fig:reconstruction_plots} illustrates representative fitted trajectories from the reconstruction experiments. The plotted models generally follow the observed annual trends, but the small number of samples means that local changes can strongly affect fitted curves. For this reason, the averaged metrics in Tables~\ref{tab:reconstruction_japan_trc} and~\ref{tab:reconstruction_united_states_trc} are more reliable than any single fitted curve for comparing reconstruction performance.
