\section[\appendixname~\thesection]{Hyperparameter Search Space}
\label{sec:appendix_a}

Suppose there are \(d\) hyperparameters \(\{ \lambda_1, \lambda_2, \dots, \lambda_d \}\). For each hyperparameter \(\lambda_j\), let its domain be \(\mathcal{D}_j = [a_j, b_j]\). The full search space is the Cartesian product
\[
	\mathcal{D} = \mathcal{D}_1 \times \mathcal{D}_2 \times \cdots \times \mathcal{D}_d .
\]

In random search, for each iteration \(t = 1, 2, \dots, T\), a hyperparameter vector
\[
	\bm{\lambda}^{(t)} = \left(\lambda_1^{(t)}, \dots, \lambda_d^{(t)}\right) \in \mathcal{D}
\]
is sampled. Each hyperparameter is sampled either uniformly or log-uniformly. For each dimension \(j = 1, 2, \dots, d\),
\begin{itemize}
	\item if \(\lambda_j\) is sampled uniformly, then
	      \[
		      \lambda_j^{(t)} \sim \mathrm{Uniform}(a_j, b_j);
	      \]

	\item if \(\lambda_j\) is sampled log-uniformly, then
	      \[
		      \log\left(\lambda_j^{(t)}\right) \sim \mathrm{Uniform}(\log a_j, \log b_j),
	      \]
	      where \(a_j > 0\) and \(b_j > 0\).

\end{itemize}
The resulting configuration set is
\[
	\mathcal{G} = \left\{ \bm{\lambda}^{(t)} \mid t = 1, 2, \dots, T \right\}.
\]

In grid search, let \(M \in \mathbb{N}\) (\(M \ge 2\)) be the number of grid points for each hyperparameter. For each dimension \(j = 1, 2, \dots, d\), a set of grid points \(\mathcal{G}_j\) is constructed as follows:
\begin{itemize}
	\item for a uniform grid,
	      \[
		      \mathcal{G}_j = \left\{ a_j + \frac{m}{M - 1}(b_j - a_j) \middle| m = 0, 1, \dots, M - 1 \right\};
	      \]
	\item for a log-uniform grid,
	      \[
		      \mathcal{G}_j = \left\{ \exp \left( \log a_j + \frac{m}{M - 1}(\log b_j - \log a_j) \right) \middle| m = 0, 1, \dots, M - 1 \right\},
	      \]
	      where \(a_j > 0\) and \(b_j > 0\).
\end{itemize}
The resulting configuration set is \(\mathcal{G} = \mathcal{G}_1 \times \mathcal{G}_2 \times \cdots \times \mathcal{G}_d\), where each configuration satisfies \(\bm{\lambda} \in \mathcal{G}\).

Table~\ref{tab:model-configurations} reports the search spaces and fixed configurations used in the experiments. Grid search used \(M=12\) points per tuned hyperparameter, and random search used \(T=128\) draws. Unless otherwise noted, tunable-model inputs and targets were standardized using parameters fitted only on the relevant training fold or partition.

\begin{table}[H]
	\centering
	\caption{Search spaces and fixed model configurations used in the experiments.}
	\small
	\begin{tabular}{p{0.20\linewidth}p{0.72\linewidth}}
		\toprule
		Model & Configuration \\
		\midrule
		Ridge regression & Polynomial degree: uniform range \([2,9]\); \(\alpha\): log-uniform range \([10^{-6},10^{3}]\). Polynomial features exclude the bias term, and ridge regression fits an intercept. \\
		GPR & RBF kernel plus white-noise kernel. Length scale: log-uniform range \([10^{-3},10^{3}]\); noise level: log-uniform range \([10^{-6},10^{1}]\). The kernel bounds are \([10^{-10},10^{3}]\) for length scale and \([10^{-6},10^{1}]\) for noise level. \\
		SVR & RBF kernel. \(C\): log-uniform range \([10^{-1},10^{2}]\); \(\epsilon\): log-uniform range \([10^{-3},1]\). SVR uses scikit-learn's fixed \texttt{gamma='scale'} setting; SVR\textsuperscript{*} additionally tunes \(\gamma\) log-uniformly over \([10^{-3},1]\). \\
		MLP & Two hidden layers of widths 32 and 16, each followed by layer normalization and ReLU; AdamW optimizer and mean-squared-error loss. The tuning grid uses batch sizes \(\{16,32,64\}\), learning rates \(\{10^{-4},3\times10^{-4},10^{-3},3\times10^{-3}\}\), and weight decays \(\{0,10^{-5},10^{-4},10^{-3}\}\), with 100 epochs per tuning trial. The selected configuration is trained for at most 500 epochs with a 10\% validation split, early-stopping patience 25, and minimum validation-loss improvement \(10^{-4}\). \\
		Isolation forest & Contamination \(0.2\) for feature-importance analysis and \(0.1\) for the pooled neural-network case study; 200 estimators; the per-run derived seed as \texttt{random\_state}. The forest is fitted to predictor variables only. \\
		Random forest & 300 estimators; unlimited maximum depth; minimum split size 2; minimum leaf size 1; scikit-learn default \texttt{max\_features=1.0}; \texttt{random\_state=471}. \\
		Baselines and statistical models & Moving-average interpolation window 3; natural cubic-spline boundary condition; Theil--Sen regression on the most recent seven training observations with \texttt{random\_state=0}; Auto ARIMA search \(p\in[0,2]\), \(d\in[0,1]\), \(q\in[0,2]\), \(p+d+q\leq5\), selected by AIC with no trend; additive-trend, non-seasonal exponential smoothing with estimated initialization and no damped trend. \\
		\bottomrule
	\end{tabular}
	\label{tab:model-configurations}
\end{table}
